{
  "name": "@mailmind/server",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "nodemon --watch src --ext ts --exec ts-node src/index.ts",
    "build": "tsc",
    "start": "node dist/index.js"
  },
  "dependencies": {
    "express": "^4",
    "cors": "^2",
    "dotenv": "^16"
  },
  "devDependencies": {
    "typescript": "^5",
    "@types/express": "^4",
    "@types/node": "^20",
    "@types/cors": "^2",
    "ts-node": "^10",
    "nodemon": "^3"
  }
}